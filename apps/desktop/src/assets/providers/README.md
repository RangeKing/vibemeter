# Provider icons

`codex.svg` and `claudecode.svg` are pinned from
[`@lobehub/icons-static-svg` 1.94.0](https://github.com/lobehub/lobe-icons),
licensed under MIT. VibeMeter stores the two SVGs locally so the Notch never
needs a network request.
